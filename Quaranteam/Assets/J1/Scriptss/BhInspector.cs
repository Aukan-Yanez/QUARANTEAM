using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;




public class BhInspector : MonoBehaviour
{
    /*
     * [CustomEditor(typeof(Blackhole))]
    public override void OnInspectorGUI()
    {
        Blackhole bh = (Blackhole)target;
        //GUILayout.Label("Los objetos a ser atraidos por la gravedad deben contar con el componente Rigidbody2D.Para ser detectados dentro de un rango deben contar a demás con un componente Collider2D.");

        GUILayout.Space(10);
        GUILayout.TextArea("Los objetos a ser atraidos por la gravedad deben contar con el componente Rigidbody2D.Para ser detectados dentro de un rango deben contar a demás con un componente Collider2D.");
        GUILayout.Space(10);

        base.OnInspectorGUI();
        
    }
    */
}
